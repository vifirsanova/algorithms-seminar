"""
Задача 1: Поиск ошибок в рекурсивной функции
"""


def factorial(n: int) -> int:
    """
    Вычисляет факториал числа рекурсивно.
    
    Аргументы:
        n: Неотрицательное целое число
        
    Возвращает:
        n!
        
    Исключения:
        ValueError: если n < 0
        
    Сложность:
        Время: O(n)
        Память: O(n)
        
    Примеры:
        >>> factorial(0)
        1
        >>> factorial(5)
        120
    """
    # TODO: Реализовать корректный факториал
    #
    # Подсказки:
    # 1. Добавьте базовый случай для n == 0
    # 2. Проверьте, что n >= 0
    # 3. Используйте рекурсивный вызов factorial(n - 1)
    pass


def factorial_safe(n: int) -> int:
    """
    Вычисляет факториал числа с проверкой ошибок.
    
    Аргументы:
        n: Неотрицательное целое число
        
    Возвращает:
        n!
        
    Исключения:
        ValueError: если n < 0 или n > 998 (глубина рекурсии)
        
    Сложность:
        Время: O(n)
        Память: O(n)
        
    Примеры:
        >>> factorial_safe(0)
        1
        >>> factorial_safe(5)
        120
    """
    # TODO: Реализовать факториал с проверками
    #
    # 1. Проверьте, что n >= 0
    # 2. Проверьте, что n <= 998 (чтобы избежать RecursionError)
    # 3. Реализуйте рекурсивный подсчёт
    pass
