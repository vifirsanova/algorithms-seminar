"""
Задача 2: Рекурсивный обход списка
"""

from typing import List, Optional


def sum_recursive(arr: List[int]) -> int:
    """
    Вычисляет сумму элементов списка рекурсивно (с использованием срезов).
    
    Аргументы:
        arr: Список целых чисел
        
    Возвращает:
        Сумма всех элементов списка
        
    Сложность:
        Время: O(n)
        Память: O(n) (из-за создания срезов)
        
    Примеры:
        >>> sum_recursive([])
        0
        >>> sum_recursive([1, 2, 3])
        6
    """
    # TODO: Реализовать рекурсивную сумму
    #
    # Базовый случай: пустой список -> возвращаем 0
    # Рекурсивный случай: arr[0] + sum_recursive(arr[1:])
    pass


def sum_recursive_opt(arr: List[int], index: int = 0) -> int:
    """
    Вычисляет сумму элементов списка рекурсивно (оптимизированная версия).
    
    Аргументы:
        arr: Список целых чисел
        index: Текущий индекс (используется для рекурсии)
        
    Возвращает:
        Сумма всех элементов списка
        
    Сложность:
        Время: O(n)
        Память: O(n) (только стек вызовов, без срезов)
        
    Примеры:
        >>> sum_recursive_opt([])
        0
        >>> sum_recursive_opt([1, 2, 3])
        6
    """
    # TODO: Реализовать оптимизированную рекурсивную сумму
    #
    # Базовый случай: дошли до конца списка (index == len(arr)) -> 0
    # Рекурсивный случай: arr[index] + sum_recursive_opt(arr, index + 1)
    pass
